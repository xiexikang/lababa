**目标**

* 重启前端开发服务，确保微信小程序与 H5 持续编译与预览正常。

**执行步骤**

* 终止已运行的前端进程（如有）：

  * 关闭任何正在运行的 `npm run dev:weapp` / `npm run dev:h5` 进程。

* 重启小程序前端：

  * 启动 `npm run dev:weapp`（watch 模式，持续输出到 `dist/`）。

  * 在微信开发者工具导入 `dist` 目录，勾选“监视文件变更”“自动编译”，点击“重新编译”。

* 可选并行重启 H5：

  * 启动 `npm run dev:h5`，浏览器访问 `http://localhost:3000/pages/index/index`。

* 验证：

  * 观察控制台编译日志是否增量刷新；

  * 页面修改（例如 `pages/profile/index.vue`）保存后，开发者工具自动同步更新。

**你确认后**

* 我将立即终止旧进程并启动 `npm run dev:weapp`（可并行启动 H5），确保不影响当前代码与配置。

