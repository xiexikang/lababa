## 目标
- 服务端仅保留新的编辑猫咪接口：`POST /api/cats/update`（鉴权、body 传 `id`）。
- 移除旧接口：`PUT /api/cats/update/:id`。

## 改动范围
- 后端（server-go/main.go）
  - 删除旧路由处理块：`mux.HandleFunc("/api/cats/update/", ...)`（约在 g:\www-xxk\xcx-lababa\server-go\main.go:1009-1082，当前显示在:1025）。
  - 保留并不修改：
    - `GET /api/cats/detail/:id`（详情预取）。
    - `POST /api/cats/update`（新的编辑保存）。
- 前端（src/pages/cats/index.vue）
  - 已使用新接口：编辑分支 `post('/api/cats/update', { id, ... })`；无需改动。

## 验证
- 重新编译 server-go 并重启服务。
- 打开编辑页：详情预取成功；保存请求走 `POST /api/cats/update` 返回最新数据。
- 确认不存在对 `PUT /api/cats/update/:id` 的调用。