## 目标
- 将统计卡片改为“两行布局”：上行仅显示三个标题，下行仅显示三个数值（含单位），与参考图一致。

## 模板改造（src/pages/profile/index.vue）
- 替换统计卡片结构为：
```vue
<view class="stats-card">
  <view class="row titles">
    <text class="cell">粑粑次数</text>
    <text class="cell">时长总计</text>
    <text class="cell">粑友数量</text>
  </view>
  <view class="row values">
    <view class="cell"><text class="num">{{ totalCount }}</text><text class="unit">次</text></view>
    <view class="cell"><text class="num">{{ totalMinutes }}</text><text class="unit">分</text></view>
    <view class="cell"><text class="num">{{ friendsCount }}</text><text class="unit">位</text></view>
  </view>
</view>
```
- 保持现有 `totalCount/totalMinutes/friendsCount` 计算逻辑不变。

## 样式调整（同文件 scoped 样式块）
```scss
.stats-card { margin: 20rpx 40rpx; padding: 24rpx 28rpx; border: 6rpx solid #2d5a3d; border-radius: 28rpx; background: #ffffe8; box-shadow: 0 6rpx 16rpx rgba(0,0,0,0.12); }
.row { display: grid; grid-template-columns: 1fr 1fr 1fr; text-align: center; }
.row.titles { margin-bottom: 12rpx; }
.row.titles .cell { font-size: 26rpx; color: #2d5a3d; opacity: .9; }
.row.values .cell { display:inline-flex; justify-content:center; align-items:baseline; gap: 8rpx; }
.num { font-size: 36rpx; font-weight: 800; color: #2d5a3d; }
.unit { font-size: 26rpx; color: #2d5a3d; opacity: .8; }
```
- 保证数值不换行：给 `.num` 与 `.unit`（或父级 `.cell`）添加 `white-space: nowrap;` 如有需要。

## 验证
- 个人中心显示为两行：上行三个标题、下行三个数值；对齐居中，字号与颜色符合参考图。
- 记录变化后数值实时更新；无记录显示 0。

确认后我将按此结构修改模板与样式并联调。