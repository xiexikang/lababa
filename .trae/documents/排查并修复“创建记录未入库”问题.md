## 后端：细化 /api/records/create 错误
- 未登录：HTTP 401，`code=401`，`msg=未登录`（已有 withAuth 返回，保持一致）。
- 缺少 `catId`：HTTP 400，`code=400`，`msg=缺少猫咪ID`（替换现有 `500`）。
- 猫不存在：HTTP 404，`code=404`，`msg=猫不存在`（`SELECT userId FROM cats WHERE id=?` 扫描不到时）。
- 归属不匹配：HTTP 403，`code=403`，`msg=猫咪不属于当前用户`（`owner != userID` 时）。
- 变更位置：`server-go/main.go:663-671` 附近，将 `writeErr` 的 `httpCode` 与 `code` 分别设置为上述值。

## 前端：创建前自动校验/选择猫
- 在 `src/store/simple.ts` 的 `saveRecord` 与 `addRecordForDate` 前，新增猫列表加载与校验逻辑：
  - 调用 `/api/cats/list` 取当前用户的猫；若为空，提示并引导到猫管理页或弹窗创建。
  - 若未显式选择 `catId`，默认使用第一个猫或弹出选择器。
  - 若传入的 `catId` 不在列表中，提示并阻止提交。
- UI 层（记录表单页）增加猫选择控件，来源于上述列表（如已有则复用）。

## 验证步骤
- 登录获取 token。
- 创建猫获取 `catId`。
- 前端选择猫后创建记录，期望：
  - 成功时：`code=0`，`data.record` 返回记录，`/api/records/list` 可查询到。
  - 失败时：收到明确错误码与信息，前端提示用户并阻止提交。

## 交付内容
- 后端：`server-go/main.go` 中错误码调整（仅影响返回，不改业务逻辑）。
- 前端：`src/store/simple.ts` 增加猫校验逻辑；相关页面增加猫选择/引导。

确认后我将按上述改动实施，并自测保证接口与前端行为符合预期。